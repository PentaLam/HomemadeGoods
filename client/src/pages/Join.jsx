import { Container} from 'react-bootstrap'
import ListingForm from '../components/ListingForm';

const Join  = () => {
    return (
<Container>
      <ListingForm/>
</Container>
    );
}

export default Join;
